import 'package:flutter/material.dart';

class LocationBigCard extends StatelessWidget {
  const LocationBigCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}