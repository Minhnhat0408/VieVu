import 'package:flutter/material.dart';

class PrefOption extends StatelessWidget {
  const PrefOption({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}